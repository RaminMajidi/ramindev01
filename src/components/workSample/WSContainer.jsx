import React from 'react'

const WSContainer = ({ children }) => {
    return (
        <section className='px-5 md:px-20 py-4'>
            {children}
        </section>
    )
}

export default WSContainer